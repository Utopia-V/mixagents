<!-- codex-deepseek-subagent:start -->
- `v4_flash_worker` is a legacy route for Codex `0.148.x` and older. On Codex `0.149.0` or later, use `$broker` from `mixagents-broker`.
- On a compatible legacy build, read `$use-v4-flash-worker` before using `v4_flash_worker`; it defines the required plaintext-Hook handoff.
<!-- codex-deepseek-subagent:end -->
